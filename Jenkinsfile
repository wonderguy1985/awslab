node {
   def mvnHome
   stage('Preparation') { 
      sh 'echo perparation'
   }
   stage('Build') {
      // Run the maven build
      sh '/usr/local/bin/packer version'
   }
   stage('Results') {
      sh 'echo finish'
   }
}
