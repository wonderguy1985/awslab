node {
   def mvnHome
   stage('Preparation') { 
      sh 'echo perparation'
   }
   stage('Build') {
      // Run the maven build
      sh 'sudo docker version'
   }
   stage('Results') {
      sh 'echo finish'
   }
}
